<%= render 'users/shared/error_messages', resource: @user %>

<main class="card mb-1" style="max-width: 480px; margin-left: auto; margin-right: auto">
  <div class="card-body">
    <h5 class="card-title">メンバー登録（β版）</h5>
    <p class="card-text"><%= render 'new_user' %></p>
  </div>
</main>
