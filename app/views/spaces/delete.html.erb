<% content_for(:html_title) { 'スペース削除' } %>

<ul class="nav nav-tabs mb-2">
  <li class="nav-item">
    <%= link_to 'スペース', space_path(@space.code), class: 'nav-link' %>
  </li>
  <li class="nav-item">
    <a data-mdb-toggle="tab" class="nav-link active">スペース削除</a>
  </li>
</ul>

<div class="card card-body mb-2" style="max-width: 720px">
  <h5 class="card-title mb-3">
    <%= render './spaces/title', space: @space, current_member: @current_member, suffix_title: nil %>
  </h5>
  <div class="card-text mb-3">
    <p>
      スペースは<%= Settings['space_destroy_schedule_days'] %>日後に削除されます。それまでは取り消し可能です。<br>
      削除されるまではアクセスできますが、一部機能が制限されます。
    </p>
    <%= button_to '削除', destroy_space_path(@space.code), data: { confirm: '本当に削除しますか？' }, method: :post, class: 'btn btn-danger' %>
  </div>
  <div class="card-footer pb-0 px-0">
    <ul class="mt-2 mb-0">
      <li><%= link_to 'スペース情報変更', edit_space_path(@space.code) %></li>
    </ul>
  </div>
</div>
