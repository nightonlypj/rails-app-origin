<% content_for(:html_title) { 'スペース削除取り消し' } %>

<ul class="nav nav-tabs mb-2">
  <li class="nav-item">
    <%= link_to 'スペース', space_path(@space.code), class: 'nav-link' %>
  </li>
  <li class="nav-item">
    <a data-mdb-toggle="tab" class="nav-link active">スペース削除取り消し</a>
  </li>
</ul>

<div class="card card-body mb-2" style="max-width: 720px">
  <h5 class="card-title mb-3">
    <%= render 'title', space: @space, current_member: @current_member %>
  </h5>
  <div class="card-text mb-3">
    <p>
      このスペースは<%= l(@space.destroy_schedule_at.to_date) %>以降に削除されます。それまでは取り消し可能です。<br>
<% if @space.destroy_requested_at.present? %>
      （<%= l(@space.destroy_requested_at) %>にスペース削除依頼を受け付けています）<br>
<% end %>
    </p>
    <%= button_to '取り消し', undo_destroy_space_path(@space.code), data: { confirm: '本当に取り消しますか？' }, method: :post, class: 'btn btn-primary' %>
  </div>
  <div class="card-footer pb-0 px-0">
    <ul class="mt-2 mb-0">
      <li><%= link_to 'スペース情報変更', edit_space_path(@space.code) %></li>
    </ul>
  </div>
</div>
