<%= render 'users/shared/error_messages', resource: @space %>

<main class="card mb-1" style="max-width: 640px; margin-left: auto; margin-right: auto">
  <div class="card-body">
    <h5 class="card-title">スペース情報変更（β版）</h5>
    <p class="card-text"><%= render 'edit_space' %></p>
    <p class="card-footer"><%= render 'links' %></p>
  </div>
</main>
