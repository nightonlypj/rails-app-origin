<%= render 'users/shared/error_messages', resource: @space, resource2: @customer %>

<main class="card" style="max-width: 640px; margin-left: auto; margin-right: auto">
  <div class="card-body">
    <h5 class="card-title">スペース作成（β版）</h5>
    <p class="card-text"><%= render 'new_space' %></p>
    <p class="card-footer"><%= render 'links' %></p>
  </div>
</main>
