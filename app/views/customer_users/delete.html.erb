<h2><%= @customer.name %>のメンバー解除</h2>

<div class="field">
  <label>表示名</label>
  <%= image_tag @customer_user.user.image_url(:small) %> <%= @customer_user.user.name %>
</div>

<div class="field">
  <label>メールアドレス</label>
  <%= @customer_user.user.email %>
</div>

<div class="field">
  <label>権限</label>
  <%= @customer_user.power_i18n %>
</div>

<p><%= button_to '解除確認', customer_user_path(customer_code: @customer.code, user_code: @customer_user.user.code), data: { confirm: '本当に解除しますか？' }, method: :delete %></p>

<ul>
  <li><%= link_to 'メンバー一覧', customer_users_path(customer_code: @customer.code) %></li>
</ul>
