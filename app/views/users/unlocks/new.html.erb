<%= render 'users/shared/alert_messages', messages: resource.errors[:unlock_token] %>
<%= render 'users/shared/error_messages', resource: resource unless resource.errors[:unlock_token].any? %>

<main class="card" style="max-width: 480px; margin-left: auto; margin-right: auto">
  <div class="card-body">
    <h5 class="card-title">アカウントロック解除</h5>
    <p class="card-text"><%= render 'new_user' %></p>
    <p class="card-footer"><%= render 'users/shared/links' %></p>
  </div>
</main>
