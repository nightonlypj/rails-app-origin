<% if user.present? %>
<%= image_tag user.image_url(:small), class: 'rounded-circle' %>
<%= user.name %>
<% if user.destroy_reserved? %>
<i class="fas fa-trash-alt ms-1" data-mdb-toggle="tooltip" data-mdb-placement="bottom" title="<%= t('%{date}以降に削除される予定です。', date: l(user.destroy_schedule_at.to_date)) %>"></i>
<% end %>
<% elsif present %>
N/A
<% end %>
