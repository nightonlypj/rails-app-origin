<div class="card card-body">
  <h5 class="card-title mb-3"><%= t('アカウント登録') %></h5>
  <div class="card-text mb-3">
    <a href="<%= new_user_registration_path %>" class="btn btn-primary text-nowrap">
      <i class="fas fa-user-plus"></i>
      <span class="ms-1"><%= t('無料で始める') %></span>
    </a>
  </div>
  <div class="card-footer pb-0 px-0">
    <nav class="mt-2 mb-0">
      <%= link_to t('ログイン'), new_user_session_path %>
    </nav>
  </div>
</div>
