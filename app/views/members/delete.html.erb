<main class="card mb-1" style="max-width: 640px; margin-left: auto; margin-right: auto">
  <div class="card-body">
    <h5 class="card-title"><%= @customer.name %>のメンバー解除</h5>
    <p class="card-text">

<% if @member.registrationed_at.present? %>
      <label class="form-label small">氏名:</label>
      <div class="mb-3">
        <%= image_tag @member.user.image_url(:small) %> <%= @member.user.name %>
      </div>
<% else %>
      <div class="note note-info mb-3">
        登録待ちのメンバーです。
      </div>
<% end %>

      <label class="form-label small">メールアドレス:</label>
      <div class="mb-3">
        <%= @member.user.email %>
      </div>

      <label class="form-label small">権限:</label>
      <div class="mb-3">
        <%= @member.power_i18n %>
      </div>

      <p><%= button_to '解除', destroy_member_path(customer_code: @customer.code, user_code: @member.user.code), data: { confirm: '本当に解除しますか？' }, method: :delete, class: 'btn btn-danger' %></p>
    </p>
    <p class="card-footer"><%= render 'links' %></p>
  </div>
</main>
