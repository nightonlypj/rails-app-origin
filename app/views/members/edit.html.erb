<%= render 'users/shared/error_messages', resource: @member %>

<main class="card mb-1" style="max-width: 640px; margin-left: auto; margin-right: auto">
  <div class="card-body">
    <h5 class="card-title"><%= @customer.name %>のメンバー権限変更</h5>
    <p class="card-text"><%= render 'edit_member' %></p>
    <p class="card-footer"><%= render 'links' %></p>
  </div>
</main>
