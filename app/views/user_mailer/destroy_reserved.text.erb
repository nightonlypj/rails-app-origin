<%= t('メール宛先', name: @user.name) %>
<%= t('メール挨拶文', app_name: t('app_name')) %>

<%= t('アカウント削除を受け付けました。またのご利用をお待ちしております。') %>

<%= t('アカウント削除取り消しメッセージ', date: l(@user.destroy_schedule_at.to_date)) %>
<%= t('アカウント削除補足') %>

<%= t('アカウント削除取り消し') %>
<% if @undo_delete_url.present? %>
<%= @undo_delete_url %>
<% else %>
<%= delete_undo_user_registration_url %>
<% end %>
