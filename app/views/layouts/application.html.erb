<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta http-equiv="x-ua-compatible" content="ie=edge" />
    <title><%= "#{t('app_name')}#{Settings['env_name']}" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/all.css" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" />
    <link rel="stylesheet" href="<%= config.asset_host %>/css/mdb.min.css?v3.1.0" />
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header style="margin-bottom: 65px">
      <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
        <div class="container-fluid">
          <%= link_to "#{t('app_name')}#{Settings['env_name']}", root_path, class: 'navbar-brand' %>
          <button class="navbar-toggler" type="button" data-mdb-toggle="collapse" data-mdb-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <i class="fas fa-bars"></i>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <ul class="navbar-nav" style="margin-left: auto">
<% if user_signed_in? %>
              <li class="nav-item text-nowrap" style="height: 45px"><%= link_to (image_tag current_user.image_url(:small)).html_safe + ' ' + current_user.name, edit_user_registration_path, class: 'nav-link d-inline-block text-truncate', style: 'max-width: 400px' %></li>
              <li class="nav-item text-nowrap"><%= link_to 'ログアウト', destroy_user_session_path, class: 'nav-link', style: 'padding-top: 10px; padding-bottom: 10px' %></li>
<% else %>
              <li class="nav-item text-nowrap"><%= link_to 'ログイン', new_user_session_path, class: 'nav-link', style: 'padding-top: 10px; padding-bottom: 10px' %></li>
              <li class="nav-item text-nowrap"><%= link_to 'アカウント登録', new_user_registration_path, class: 'nav-link', style: 'padding-top: 10px; padding-bottom: 10px' %></li>
<% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
<% if destroy_reserved_message? %>
    <aside>
      <p class="note note-warning m-1">このアカウントは<%= l(current_user.destroy_schedule_at.to_date) %>以降に削除されます。<%= link_to '取り消しはこちら', delete_undo_user_registration_path %></p>
    </aside>
<% end %>
<% if alert.present? %>
    <aside>
      <p class="note note-danger m-1"><%= alert %></p>
    </aside>
<% end %>
<% if notice.present? %>
    <aside>
      <p class="note note-success m-1"><%= notice %></p>
    </aside>
<% end %>

<%= yield %>

    <footer class="bg-light">
      <div class="text-center p-3">
        Copyright &copy; <%= link_to t('my_name'), t('my_url'), target: :_blank, rel: [:noopener, :noreferrer] %> All Rights Reserved.
      </div>
<% if Rails.env.development? %>
      <h5>development</h5>
      <ul>
        <li><%= link_to 'LetterOpenerWeb', letter_opener_web_path %></li>
        <li><%= link_to 'ActionMailer Preview', '/rails/mailers' %></li>
        <li><%= link_to 'RailsAdmin', rails_admin_path %></li>
      </ul>
<% end %>
    </footer>
  </body>
  <script type="text/javascript" src="<%= config.asset_host %>/js/mdb.min.js?v3.1.0"></script>
</html>
